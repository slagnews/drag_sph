# SPH simulation
The C++ source code is located in the src/ directory. By going into this directory and running
```
python3 setup.py build_ext --inplace
```
the python wrapper is made and the C++ code can be used.